# 2026 年挑战杯揭榜挂帅

## 基于跨领域退化数据迁移的航天器关键组件寿命预测方法研究

## 官方要求与项目执行约束

> 本文件是项目内部的统一约束源。后续方案、数据、代码、实验和报告均应先对照本文件检查。
>
> 整理日期：2026-08-10
>
> 当前项目场景：电池 + 反作用轮

## 0. 使用规则

本文件将信息分为五类，避免把示例内容误当成比赛硬要求：

- **[硬要求]**：赛题方案中明确要求，必须满足。
- **[官方口径]**：FAQ 或官方参考资料对赛题的解释，必须遵守。
- **[模板约束]**：技术方案报告模板要求报告中说明的内容。
- **[样例示意]**：技术报告样例中的展示方式或虚构参数，只能参考结构，不能直接当作实际成果。
- **[项目决定]**：本团队已经确定的范围；不得无理由扩展或改变。

除非后续获得新的官方书面说明，出现冲突时按以下优先级处理：

`赛题方案硬要求 > 官方 FAQ 口径 > 官方参考资料 > 报告模板约束 > 技术报告样例`

## 0.1 通用问题处理与决策原则

以下原则适用于数据、仿真、参数、退化机理、标签、模型、实验、工程结构和报告，不针对某一个具体问题：

1. **先定义问题，再选择方案。** 先明确目标、边界、评价标准和必须交付的证据，不能因为先找到某个数据、参数或工具，就反过来让项目迁就它。
2. **重要决定必须比较候选方案。** 至少从来源可信度、信息完整性、内部一致性、当前任务适用性、可验证性、实现代价和后续风险进行比较，不能把“更官方”“更常见”或“最先找到”等同于“更合适”。
3. **缺少信息时先查证，不能急于补假设。** 应继续查找替代资料、已有研究、官方接口或可复核方法；确实无法获得时，才允许形成明确标注的项目假设，并说明影响、敏感性和替换条件。
4. **证据层必须分开。** 官方事实、公开观测、文献规律、代码推导、项目决定、工程近似和待验证假设必须分别记录，不能通过措辞把低等级证据包装成高等级事实。
5. **能够运行不等于方案正确。** 重要结果必须经过格式检查、数值检查、物理或业务合理性检查，并尽可能使用独立公式、另一实现、公开基准或敏感性实验进行交叉验证。
6. **冻结前主动寻找反例。** 在确定关键数据、参数、模型或路线前，要检查是否存在更完整的来源、更合适的方案、数据泄漏、捷径特征、负迁移、边界失效或无法解释的结果。
7. **不确定性必须显式保留。** 无法确认的内容应标记为未冻结或待验证，不能为了让方案看起来完整而给出没有依据的确定值。
8. **核心决定变更后必须做影响审计。** 搜索并检查相关配置、代码、数据产物、文档、图表、结果和结论，重新运行受影响的验证，不能只修改最先发现的一个文件。
9. **同时记录选择理由和淘汰理由。** 记录为什么采用当前方案、其他候选为什么不适合以及未来在什么条件下需要重新评估，避免后续成员重复走错路线。
10. **结论强度不得超过证据强度。** 仿真结果只能证明所选模型和边界内的行为；公开数据不能自动代表真实航天飞行状态；单次实验不能证明普遍有效。

统一决策流程为：

```text
问题与验收标准
    -> 约束和证据分层
    -> 候选方案收集
    -> 完整性/一致性/适用性比较
    -> 实现与独立验证
    -> 明确边界后冻结
    -> 全项目影响审计
```

## 1. 官方材料清单

| 材料 | 作用 |
| --- | --- |
| `C:\Users\联想\Desktop\XA-202608中国科学院微小卫星创新研究院-基于跨领域退化数据迁移的航天器关键组件寿命预测方法研究比赛方案.pdf` | 赛题背景、任务、交付物、评分、时间和提交要求 |
| `C:\Users\联想\Desktop\基于跨领域退化数据迁移的航天器关键组件寿命预测方法研究比赛—参考资料.pdf` | RUL/PHM、迁移学习文献，公开数据集和仿真工具入口 |
| `C:\Users\联想\Desktop\赛事问题FAQ.docx` | 数据、评测、仿真、工具和创新边界的官方答复 |
| `C:\Users\联想\Desktop\技术方案报告模板.docx` | 报告章节、必写证据、复现清单和写作边界 |
| `C:\Users\联想\Desktop\技术报告文档样例.docx` | 参考技术路线、图表和工程表达；其中数值、规模、代码和结果均为虚构示例 |

## 2. 项目任务合同

### 2.1 一句话定义

在真实航天器长期在轨退化样本不足的条件下，使用其他领域的公开退化数据进行源域训练，再通过航天器目标域仿真数据完成适配，预测电池和反作用轮的渐进退化趋势或剩余可用寿命（RUL），并交付可完整复现的 PyTorch 工程。

### 2.2 当前项目决定

- **[项目决定] 场景一：电池**
  - 关注循环老化和日历老化造成的可用容量下降、内阻上升。
  - 预测健康状态、未来退化趋势和/或 RUL。
- **[项目决定] 场景二：反作用轮**
  - 关注轴承润滑劣化、磨损造成的摩擦逐步增大。
  - 预测摩擦退化趋势、健康状态和/或 RUL。
- **[硬要求]** 两个场景均应是渐进式、长期退化预测，不能退化成单纯的瞬时故障分类或阈值报警。

### 2.3 官方输出边界

比赛方案原文使用“寿命或长期退化趋势”“剩余寿命或性能衰减过程”“未来退化趋势或剩余可用寿命”。因此：

- **[官方硬要求]** 每个场景必须完成渐进式长期退化建模，并输出长期退化趋势或剩余可用寿命中的至少一种；
- **[条件要求]** 只有选择普通 RUL 作为输出时，才必须先给出目标域自身有依据、可执行的 EoL 判据和时间尺度；
- **[非硬要求]** 未选择普通 RUL 的趋势/HI 首版，不要求为了制造标签而强行设定或观测 EoL，但必须说明观测终点、证据边界和不能输出 RUL 的原因；
- **[项目当前决定]** 电池首版输出 HI 与退化趋势，EoL/RUL 暂不可用；反作用轮保留已有任务层 EoL 与 RUL。

## 3. 赛题硬性要求与评分

### 3.1 必须完成的技术链路

```text
公开跨领域退化数据
    -> 源域模型训练
    -> 航天器运行/退化/故障仿真
    -> 航天目标域数据生成
    -> 迁移微调或适配
    -> 退化趋势/HI，或在具备目标域 EoL 时预测 RUL
    -> 误差、稳定性、提前性和对比验证
    -> 可复现工程交付
```

### 3.2 必须提交的成果

- 技术方案报告；
- 公开退化数据集及获取、版本和许可说明；
- 自建航天仿真数据集及生成方法说明；
- 基于 PyTorch 的可执行算法代码；
- 能完整复现训练、迁移微调、预测和评估的说明文档；
- Docker 封装属于重要工程加分项，建议作为交付的一部分。

### 3.3 评分结构

| 维度 | 分值 | 必须提供的证据 |
| --- | ---: | --- |
| 问题建模与仿真场景设计 | 30 | 退化机理、可观测量、长期工况、数据生成过程 |
| 工程实现与可复现性 | 50 | 完整代码和数据、PyTorch、运行说明、Docker、独立复跑 |
| 算法效果、迁移能力与应用表达 | 20 | 公开数据训练到航天数据适配、基线对比、误差/稳定性/提前性 |
| 应用表达加分 | +5 | 健康状态、退化过程、告警和工程流程的直观表达 |

**前置门槛：**代码不能成功运行并复现主要结果时，工程实现与可复现性维度的其余分值不再计入。Docker 是 8 分评分项，但不是参赛资格门槛。

### 3.4 时间与未决提交口径

赛题方案给出的节点为：作品提交截止 2026-08-15，2026-08-31 前完成初审，2026 年 9 月完善作品，2026-09-30 前参加终审。

赛题方案第八部分同时出现“RAR 邮件提交”和“通过大赛申报系统提交、不接受邮件等其他方式”的表述。提交渠道存在官方文件内部冲突，必须向赛事方确认后再按确认结果执行，不能自行猜测。

## 4. 两个场景的范围定义

### 4.1 电池场景

#### 必须说明

- 任务工况：轨道日照/阴影、充放电制度、载荷功率、温度和运行周期；
- 退化状态：可用容量 `Q` 下降、欧姆内阻 `R0` 上升，或其他有依据的等效退化状态；
- 遥测依据：电荷守恒、等效电路、电流引起的压降和 `I^2R` 热损耗关系；
- 在线输入：电压、电流、温度、SOC 估计、日照状态、载荷功率、放电深度、末端电压等，按实际方案取舍；
- 预测目标：电池 HI/SOH、未来容量或电压趋势、RUL 中至少选择一种长期预测目标，可附加不确定性；
- EoL 判据：仅当输出普通 RUL 时，必须给出有依据的容量、末端电压或任务供电能力判据；趋势/HI 路线不得为制造 RUL 强设 EoL。

#### 不能混淆

- `Q`、`R0` 等仿真真值可以用于生成标签和校核，但不能在预测阶段作为在线输入；
- 不能直接给端电压叠加一条下降曲线，退化应先作用于电池等效参数，再由任务负载计算电压响应；
- 样例中的 `Q/Qrated <= 80%`、参数范围和轨迹规模是虚构示例，正式项目必须重新给出来源和依据。

#### 官方资料中的数据与工具方向

- 源域可查 NASA PCoE Battery、NASA Li-ion Battery Aging 等公开电池退化数据；
- 目标域可由 Basilisk、STK 或自建任务/电源仿真与电池退化模型共同生成；
- STK 主要提供轨道和任务级工况；电池退化、遥测以及选用 RUL 时所需的目标域 EoL/RUL 标签仍需自行构建。

### 4.2 反作用轮场景

#### 必须说明

- 任务工况：姿态机动、外扰力矩、轮速范围、温度和累计运行时间；
- 退化状态：黏性摩擦、库仑摩擦或其他有依据的摩擦/轴承退化状态；
- 遥测依据：转矩平衡、电机电流、摩擦功、热平衡和姿态跟踪性能关系；
- 在线输入：轮速、指令/实际转矩、电机电流、轴承或底板温度、姿态跟踪误差等，按实际方案取舍；
- 预测目标：摩擦退化 HI、趋势、RUL 中至少选择一种长期预测目标，可附加故障模式概率；
- EoL 判据：仅当输出普通 RUL 时，必须给出摩擦性能或连续姿态任务性能超限的明确判据；当前反作用轮已采用任务层 EoL。

#### 不能混淆

- 摩擦系数、摩擦转矩和注入速率等内部仿真状态不能直接作为在线预测输入；
- 退化注入应修改物理参数、力矩路径或内部状态，再由闭环动力学生成电流、温度和跟踪误差；
- 慢摩擦漂移、突发摩擦增大、传感器开短路和错误指令应区分，寿命预测重点仍是渐进摩擦漂移；
- 样例中的摩擦阈值、参数范围和轨迹规模是虚构示例，不是官方固定指标。

#### 官方资料中的数据与工具方向

- 源域可查 XJTU-SY Bearing、FEMTO/PRONOSTIA 等轴承全寿命退化数据；
- Basilisk 的航天器动力学、姿态控制和 `reactionWheelStateEffector` 可用于构建反作用轮目标域；
- 仿真工具只提供运行环境和动力学接口；摩擦退化、遥测以及选用 RUL 时所需的目标域 EoL/RUL 标签需要自行定义。

## 5. 航天仿真数据约束

### 5.1 仿真必须形成闭环

报告要交代仿真平台、版本、模型来源、二次开发模块、消息/数据接口、动力学积分步长、遥测采样周期、退化状态更新周期、寿命加速原则和工况空间。

推荐的证据链是：

```text
任务/控制器指令
    -> 执行机构或电源动力学
    -> 部组件内部状态
    -> 退化参数更新
    -> 传感器模型
    -> 消息记录器
    -> 重采样/特征生成
    -> 数据文件与标签
```

不能只生成一条理想退化曲线，再把它伪装成航天遥测。

### 5.2 数据组织要求

赛题不限定唯一格式，但数据至少要能表达：

- 时间索引；
- 遥测量名称或字段；
- 观测值、单位和采样频率；
- 组件、设备或工况标识；
- 退化状态、HI、趋势或 RUL 标签；
- 缺测规则和掩码；
- 训练、验证、测试划分方法；
- 标签生成依据。

必须提供数据字典。公开数据还要记录版本、许可、URL、访问日期、原始文件哈希和解析后清单哈希；建议统一写入 `manifest.json`。

### 5.3 在线量、派生量和真值边界

- 仿真系统已经计算并返回、且实际预测时能够获得的派生遥测量可以作为输入；
- 必须说明它的来源、计算或测量链路、采样特性和与退化的关系；
- 不得使用实际预测时无法获得的内部状态；
- 不得使用由标签反推得到的变量；
- 建议同时保存 `truth`、`clean`、`noisy` 和 `mask`，但模型输入只能使用规定的在线可用量。

### 5.4 划分、切窗和归一化

- 先按完整设备或完整轨迹划分训练、验证和测试，再进行滑动窗口切分；
- 归一化统计量只能由训练集计算；
- 测试应包含不同工况、缺测和至少一部分未见条件，用于验证泛化；
- 不得让同一轨迹的相邻窗口跨越训练集和测试集造成数据泄漏。

### 5.5 仿真可信度与敏感性

必须进行至少一类可复核校核：方向性、边界、守恒/能量关系、专家规则、公开试验趋势或其他工程依据。

还要对关键退化参数、热参数、负载参数和传感器噪声做敏感性分析，并明确哪些规律可以被当前仿真支持，哪些绝对寿命仍需真实长寿命试验或在轨数据校准。样例中的具体扰动百分比只是示意，不是固定官方数值。

## 6. 模型与跨域迁移约束

### 6.1 迁移的对象

不能直接假设源域和目标域的原始传感器字段、量纲、采样周期和寿命阈值相同。应说明二者共享的可迁移知识，例如：

- 退化阶段；
- 退化方向；
- 变化速率或斜率；
- 拐点；
- 工况敏感性。

目标域 RUL 必须始终依据航天场景自己的 EoL 判据重新计算。

### 6.2 模型结构要求

模板允许模型结构自由选择，但报告必须说明：

- 输入窗口长度、采样和工况编码；
- 各域输入投影或字段处理；
- 共享时序编码器；
- 电池和反作用轮的场景专属预测头；
- HI、趋势、RUL、不确定性或故障模式输出；
- 训练目标、约束和模型选择依据。

样例采用“域专属输入投影 + 共享 TCN/注意力编码器 + 场景专属头”，这是参考结构，不是必须照搬的网络配置。

### 6.3 训练与适配证据

至少要能区分：

- 源域预训练获得了什么退化表征；
- 目标域适配更新了哪些模块；
- 适配使用了什么目标域数据和训练策略；
- 迁移相对于从零训练是否有效；
- 是否发生负迁移，以及发生时如何处理。

可以采用冻结后解冻、特征对齐、条件对齐、样本重加权、物理约束或其他方法，但不能只写算法名称而不说明作用位置和验证证据。

### 6.4 约束与适用边界

根据实际模型说明：

- HI 的单调性或阶段约束；
- 若输出 RUL，则施加 RUL 非负约束；
- 能量关系、任务阈值或物理边界；
- 区间估计、概率头、分位数回归或模型集成；
- 负迁移判断阈值和回退策略；
- 已覆盖的工况、数据质量范围和明确不处理的情况。

## 7. 实验与结果约束

### 7.1 对照实验

赛题要求至少与一种现有寿命预测或退化建模方法比较。模板和样例展示了线性外推、LSTM、从零训练模型、源域直推、预训练后微调和完整适配等设置，但具体组合应以实际模型和数据为准。

所有方法应使用相同的轨迹划分、输入信息、预测任务和评估脚本，保证对比公平。

### 7.2 指标口径

每个指标必须说明：含义、公式或计算过程、单位、统计范围、优劣方向和选择理由。

至少围绕以下能力组织结果：

- 未来退化趋势或 HI 预测误差；
- 若输出 RUL，则报告 RUL 预测误差；
- 趋势一致性；
- 稳定性和重复试验波动；
- 若已定义 EoL/RUL，则可报告相对仿真 EoL 的提前预警量；
- 缺测和未见工况下的鲁棒性。

“提前检测”与“到寿命终点的 RUL”不是同一个时间口径，不能混用外部项目的 KPI。

### 7.3 消融、失败案例和统计

建议报告迁移对齐、单调性/物理约束、缺测处理和目标域标签比例等消融结果，并展示至少一个失败或风险案例。神经网络结果应采用多个随机种子报告均值和波动，不能只报一次最优结果。

报告中的所有数字必须来自实际代码输出，不得使用样例中的虚构指标。

## 8. 工程复现与交付约束

### 8.1 推荐工程边界

模板和样例建议按配置驱动组织以下职责：

```text
configs       配置和实验参数
simulation    航天目标域数据生成
datasets      数据解析、轨迹划分和窗口化
models        共享编码器和场景专属头
train         源域预训练
adapt         目标域迁移/微调
evaluate      指标、对照和报告生成
visualize     图表生成
tests         数据、标签和流程检查
```

目录名称可以调整，但职责必须能够定位，且配置、随机种子、环境、代码版本、模型检查点和指标结果可追溯。

### 8.2 复现交付物

- 数据下载/解析脚本和数据版本；
- 航天仿真脚本、配置、随机种子和生成日志；
- PyTorch 预训练、适配、预测和评估代码；
- Dockerfile、依赖锁和一键复现命令；
- 模型权重、指标 JSON 和图表生成脚本；
- README、部署说明和使用说明；
- 独立复跑记录、资源测试和结果偏差；
- 报告中的示例文字、提示框和占位符全部删除。

正式验收应尽量由非主要开发成员在干净环境执行，并记录运行环境、耗时、峰值资源、主要结果和复跑偏差。

### 8.3 可选应用层

健康看板、预测服务、数据管理、FastAPI、前端、大模型辅助解释或知识检索属于可选扩展。它们不能替代核心的仿真、迁移、实验和复现证据。大模型不得直接修改寿命预测数值；若使用，必须说明输入、约束、输出验证和实际增益。

## 9. 技术方案报告约束

### 9.1 摘要

原则上控制在 300 字以内，说明核心问题、总体路线、三项主要贡献及验证证据。摘要中的三项贡献必须与 `2.3 关键贡献、创新性与验证路径` 一致，不能写成背景综述、方法清单或交付清单。

### 9.2 正文写法

- 第 1 章定义工程问题，不逐条复述赛题全文；
- 每个场景都要按“对象与工况 -> 退化机理 -> 遥测依据 -> 预测目标 -> 观测终点/证据边界”闭环说明；选择 RUL 时再增加目标域寿命终点定义；
- 第 3 章说明仿真如何生成数据和标签；
- 第 4 章说明源域/目标域差异、迁移知识和适配方法；
- 第 5 章只写实际实验条件、结果和解释；
- 第 6 章提供工程结构、命令、资源和独立复跑证据；
- 第 7 章区分任务完成情况、局限、外部事实和待验证设想。

### 9.3 模板和样例的使用边界

- 模板中的浅黄色示例段落、蓝色/灰色提示框和“提交前删除”内容必须删除并用真实内容重写；
- 二级标题可以按实际方案调整，但不能因此省略关键证据；
- 样例中的数据规模、参数、模型配置、指标、Docker 标签、仓库提交号、硬件和复跑时间均标为虚构示例，不能直接写入正式报告；
- 样例中的图表用于展示证据组织方式，不代表我们的真实实验结果；
- 外部文献、数据集、软件能力和外部 KPI 必须注明来源、版本、许可、URL 和访问日期；
- 已实现、已测试、官方事实、虚构示例和待验证设想必须分开表述。

## 10. 禁止偏离路线的事项

以下做法与官方文件要求冲突：

- 只做瞬时异常检测、重启计数或阈值报警，不做渐进退化/RUL 预测；
- 只使用公开数据，不构建航天目标域仿真数据；
- 只生成理想退化曲线，不经过物理参数、运行工况和传感器链路；
- 把摩擦真值、容量、内阻等预测时不可得的内部状态直接作为模型输入；
- 在窗口切分后再划分训练和测试；
- 用源域的寿命阈值直接定义航天目标域 RUL；
- 只写“使用迁移学习、MMD、DANN”等名称，不交代迁移对象、作用位置和验证方式；
- 只报告单次实验或样例虚构指标，不做公平基线和必要的稳定性分析；
- 用前端看板、大模型或概念演示替代核心仿真、迁移和复现证据；
- 把技术报告样例中的虚构数据、参数、结果、复跑记录写成已完成成果。

## 11. 当前未冻结的项目参数

公开源域已经冻结为 NASA PCoE Battery Data Set 和 XJTU-SY Bearing Datasets，当前版本、哈希、派生规则和产物见 `data/source_domain_freeze_manifest.json`。若后续更换源数据或规则，必须生成新的冻结版本，不能覆盖当前记录。

航天仿真平台已选择 Basilisk `bsk==2.11.1`，环境路线见 `configs/basilisk_environment_policy.json`。Python 3.12.10 项目隔离环境已经建立并通过核心导入检查。`scenarioBasicOrbit`、`scenarioAttitudeFeedbackRW`、`scenarioAttitudeFeedbackRWPower` 和 `scenarioPowerDemo` 已完成受控非可视官方示例验收；`scenarioPowerDemo` 在本机需要通过官方变量 `BSK_SUPPORT_DATA_CACHE` 指向 ASCII 支持数据路径。活动主轨道使用 `configs/target_domain_orbit_baseline.json` 中冻结的 2026 TLE，已通过 SGP4/TEME 到 J2000 转换和一圈日照验证；早期不完整轨道假设的数值文件及其归档已从项目工作目录删除，只保留“不允许重建或与 2026 TLE 拼接”的拒绝理由。健康三轴天底指向闭环已按 `configs/target_domain_attitude_control_baseline.json` 在完整一圈 2026 轨道上通过全部验收，结果见 `simulation/validation/healthy_attitude_control_2026_one_orbit_validation.json`。该结果只确认无扰动健康控制链、初始化和候选增益成立；BCT RWP015 是否适合姿态机动、外扰和长期动量积累仍未验证，不能表述为最终硬件选型或 Proba-V 飞行参数。代表性健康机动要求候选见 `configs/target_domain_attitude_maneuver_requirements.json`：首个名义测试点为连续单轴 `10 deg/600 s` 重定向、连续 `120 s` 稳定检查和 `600 s` 驻留，全部属于项目约定，不是外部飞行指标。连续参考轨迹和健康闭环名义机动均已通过验证，`+500/-500/+500 rpm`现冻结为后续任务仿真的暂定健康候选。Basilisk软件预设的 `5e-5 N m`纯库仑摩擦敏感性案例未通过稳定门，未接受为活动健康摩擦基线。随后完成的三组小积分候选配对矩阵中，无摩擦案例全部通过，但摩擦案例全部失败；最佳首轮候选仍有 `0.75213°`最大姿态误差，且峰值轮速升至 `4385.77 rpm`。因此没有接受积分参数，正式 `Ki=-1`保持不变，也不得未经批准继续扩大扫参。厂家摩擦标定仍未查到，无摩擦健康基线继续保留。项目专属渐进退化模型和目标域数据仍未开始。目标域名义场景边界见 `configs/target_domain_nominal_scenario_boundary.json`。

下列事项仍必须在后续方案中有来源、有依据、有版本记录；在冻结前不得凭样例数字直接确定：

- 两个场景的具体遥测字段、单位、采样周期和退化更新周期；
- 两个场景的退化参数范围和加速原则；对选择 RUL 的场景还必须冻结目标域 EoL 判据；
- HI、趋势、RUL、不确定性和故障模式的最终输出组合；
- 模型结构、迁移损失、约束、基线和评价指标的最终组合；
- 训练/验证/测试轨迹数量、随机种子和复跑资源；
- 最终作品提交渠道的官方确认结果。

## 12. 提交前总检查清单

- [ ] 电池和反作用轮两个渐进退化场景均已覆盖；
- [ ] 公开源域数据、版本、许可、URL、访问日期和哈希已记录；
- [ ] 航天目标域仿真脚本、配置、随机种子和生成日志可运行；
- [ ] 在线遥测、仿真真值、标签和缺测掩码边界清楚；
- [ ] 每个场景的退化机理、预测目标和观测终点/证据边界形成闭环；选择 RUL 的场景另有目标域 EoL 判据；
- [ ] 数据按完整设备/轨迹划分，切窗和归一化没有泄漏；
- [ ] 完成公开数据训练到目标域适配的完整链路；
- [ ] 至少有一个公平的现有方法对照；
- [ ] 报告了误差、趋势一致性、稳定性、提前性和适用边界；
- [ ] 做了必要的消融、缺测/未见工况或失败案例分析；
- [ ] PyTorch 训练、适配、预测、评估流程可完整复跑；
- [ ] Docker、依赖锁、README 和独立复跑记录已准备；
- [ ] 报告模板提示框、示例文字和占位符已删除；
- [ ] 所有正文数字和图表均可追溯到代码输出；
- [ ] 已区分官方事实、实际实验结果、假设和待验证设想；
- [ ] 提交渠道已向赛事方确认。
